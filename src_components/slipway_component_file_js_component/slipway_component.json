{
  "publisher": "slipwayhq",
  "name": "component_file_js",
  "description": "Requests a file from another component in js.",
  "version": "0.0.1",
  "input": {
    "properties": {
      "handle": { "type": "string" },
      "path": { "type": "string" },
      "file_type": {
        "enum": ["text", "binary"]
      }
    }
  },
  "output": {
    "optionalProperties": {
      "text": { "type": "string" },
      "bin": { "elements": { "type": "uint8" } }
    }
  }
}
